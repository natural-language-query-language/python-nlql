# Ingestion

`nlql.ingest` — The write pipeline: `Normalizer` → pluggable `Splitter` → `Embedder` (with cache) → `Indexer`.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/ingest.md).
