# Reranker

`nlql.rerank` — The optional second stage of retrieval: a pluggable `Reranker` protocol that precisely scores each `(query, passage)` pair after recall.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/rerank.md).
