# Embedder

`nlql.embed` — The `Embedder` protocol and backends, multimodal support (`MultimodalEmbedder`), and write-time caching (`EmbeddingCache` / `CachedEmbedder`).

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/embed.md).
