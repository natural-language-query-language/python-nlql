# Executor

`nlql.exec` — `Evaluator` (expression evaluation) and `Executor` (runs recall → filter → rerank → limit).

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/exec.md).
