# Errors

`nlql.errors` — A typed exception hierarchy rooted at `NLQLError`, letting callers catch by error stage.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/errors.md).
