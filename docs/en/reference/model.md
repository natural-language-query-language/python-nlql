# Data model

`nlql.model` — Modality-agnostic data model: `Payload`, `Document`, `Unit`, `Vector`.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/model.md).
