# Loaders

`nlql.loaders` — Document loaders: the `Loader` protocol plus dispatch by extension (`.txt` / `.md` / `.docx` / `.pdf`).

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/loaders.md).
