# Store

`nlql.store` — The `Store` interface, `StoreCaps` capability descriptors, and the built-in `LocalStore`. Each backend implements `Store`.

The full API reference (including the backend adapter table) is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/store.md).
