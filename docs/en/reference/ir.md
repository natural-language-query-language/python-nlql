# Query IR

`nlql.ir` — The Query IR: the canonical, JSON-serializable query AST. The common target of all three query entry points. `query_json_schema()` exports the JSON Schema for LLM function-calling.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/ir.md).
