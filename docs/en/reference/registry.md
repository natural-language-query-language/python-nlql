# Registry

`nlql.registry` — The unified capability registry for functions, splitters, embedders, and modalities, with parent-child scoped registries.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/registry.md).
