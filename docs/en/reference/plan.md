# Planner

`nlql.plan` — Query planning: score extraction, `QueryPlan`, and the splitting of filter operations that decides what can be handed to the backend.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/plan.md).
