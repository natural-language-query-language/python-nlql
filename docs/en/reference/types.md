# Types

`nlql.types` — The type system: the `TypeTag` enum and function `Signature`, plus operand coercion.

The full API reference is rendered from the Python docstrings (which are in English) on the [default-language page](/reference/types.md).
