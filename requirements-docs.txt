# Documentation build dependencies — mirrors the [project.optional-dependencies] docs extra.
# Install with: pip install -r requirements-docs.txt  (or: pip install -e ".[docs]")
mkdocs>=1.6.0
mkdocs-material>=9.5.0
mkdocstrings[python]>=0.26.0
mkdocs-static-i18n>=1.2.0
